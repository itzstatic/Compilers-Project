package ast;


public enum TypeId {
	INT, 
	FLOAT,
	VOID
	
	//Returns the max (i.e. promotion)
//	public static TypeId max(TypeId a, TypeId b) {
//		if (a.rank > b.rank) {
//			return a;
//		}
//		return b;
//	}
}
